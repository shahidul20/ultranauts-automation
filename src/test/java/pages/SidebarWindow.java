package pages;

import helpers.PageHelper;
import org.openqa.selenium.WebDriver;

public class SidebarWindow extends PageHelper {
    public SidebarWindow(WebDriver webDriver) {
        super(webDriver);
    }
}
